# fastAPI-starter-template
A reusable template that can be used for API development using fastAPI that adheres to strong architectural patterns to ensures the applications scalability and modularity.
